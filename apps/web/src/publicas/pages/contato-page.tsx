import { PublicShell } from "@/publicas/components/public-shell";

export function ContatoPage() {
  return (
    <PublicShell eyebrow="Dominio publicas" title="Contato e Inscricao">
      <p className="max-w-2xl text-slate-300">
        Estrutura inicial para formulario visual, WhatsApp e mapa.
      </p>
    </PublicShell>
  );
}
